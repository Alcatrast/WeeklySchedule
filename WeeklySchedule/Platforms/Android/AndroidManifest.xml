<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<application android:allowBackup="true" android:icon="@mipmap/appicon" android:roundIcon="@mipmap/appicon_round" android:supportsRtl="true"></application>
	<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
	<!-- Точность будильника, а не право на доставку: без обоих разрешений
	     уведомления приходят, но с задержкой в несколько минут. USE_EXACT_ALARM
	     (Android 13+) выдается при установке молча, SCHEDULE_EXACT_ALARM
	     пользователь выдает вручную и нужен фактически только на Android 12.
	     Google Play разрешает первое только будильникам и календарям, но
	     приложение ставится напрямую из apk, а не через стор -->
	<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
	<uses-permission android:name="android.permission.USE_EXACT_ALARM" />
	<!-- Будильники стираются при перезагрузке телефона: без этого разрешения
	     BootReceiver не получит ACTION_BOOT_COMPLETED и не восстановит их -->
	<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
	<!-- Разрешения для чтения файлов (Excel) -->
	<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
</manifest>